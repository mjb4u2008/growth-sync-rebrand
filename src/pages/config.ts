/**
 * Site-wide page configuration.
 *
 * CALENDLY_URL: leave empty to render the branded intake form on /book-a-call.
 * When populated with a real Calendly inline URL (e.g.
 * "https://calendly.com/your-handle/intro"), the page renders the Calendly
 * inline widget instead.
 */
export const CALENDLY_URL = "";

/** Replace before launch. */
export const CONTACT_EMAIL = "hello@growthsync.app";

/** Effective date for legal pages. Replace before launch. */
export const LEGAL_EFFECTIVE_DATE = "TBD. Set before launch";
